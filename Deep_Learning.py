import pandas as pd
import numpy as np
import datetime as dt
import time as time
import os
from keras.models import Sequential
from keras.layers import Dense, Activation

model = Sequential()
model.add(Dense)

